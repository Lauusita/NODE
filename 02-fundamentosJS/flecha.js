function sumar (a, b){
    return a+b;
}

console.log(sumar(5, 10));
// función tradicional

const restar = (c, d) => c-d

console.log(restar(5, 10));
// función de flecha

const saludar = () => "Hola Mundo";
console.log(saludar());